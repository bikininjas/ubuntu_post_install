################################################################################
# Configuration pour Ubuntu Post-Install
################################################################################
# 
# INSTRUCTIONS:
# 1. Copiez ce fichier: cp .env.example .env
# 2. Remplissez les valeurs ci-dessous
# 3. Lancez l'installation: sudo ./post_install.sh
#
# Si le fichier .env existe, les valeurs seront utilisées automatiquement.
# Sinon, le script demandera les informations de manière interactive.
#
################################################################################

# ============================================================================
# UTILISATEUR SYSTÈME
# ============================================================================
# Nom d'utilisateur principal à créer (si inexistant)
TARGET_USER=mon_user

# Mot de passe pour l'utilisateur (laissez vide pour génération automatique)
# Si vide et mode non-interactif, un mot de passe sera généré dans /root/.${TARGET_USER}_initial_password.txt
TARGET_USER_PASSWORD=

# ============================================================================
# CONFIGURATION GIT
# ============================================================================
# Nom d'utilisateur Git (pour git config --global user.name)
GIT_USER=mon_user_git

# Email Git (pour git config --global user.email)
GIT_EMAIL=mon_user@gmail.com

# ============================================================================
# DOMAINE ET SSL
# ============================================================================
# Nom de domaine principal (sans www)
# Exemple: monserveur.com
SERVER_DOMAIN=mon_user.fr

# Email pour Let's Encrypt (notifications d'expiration)
LETSENCRYPT_EMAIL=mon_user@gmail.com

# Confirmer que le DNS est configuré (yes/no)
# Mettre "yes" si le domaine pointe déjà vers ce serveur
DNS_CONFIGURED=yes

# ============================================================================
# SÉCURITÉ RÉSEAU
# ============================================================================
# Adresse IP autorisée pour SSH (format: xxx.xxx.xxx.xxx)
# Cette IP sera la SEULE autorisée à se connecter en SSH (port 22)
# ATTENTION: Assurez-vous que cette IP est correcte!
ALLOWED_SSH_IP=99.99.99.99

# ============================================================================
# OPTIONS AVANCÉES
# ============================================================================
# Répertoire pour les dépôts Git de l'utilisateur
GITREPOS_DIR=/home/${TARGET_USER}/GITRepos

# Répertoire pour les serveurs de jeu (LGSM)
GAME_DIR=/home/${TARGET_USER}/gameservers
